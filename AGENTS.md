# 開発ガイド

## 対象・優先順位

MTFAutoTrader v2.45のMQL5配布物。src/の本体・Worker・ヘッダー13ファイルを一体として扱う。利用仕様は[README_JA.md](README_JA.md)、テスト手順・限界は[VALIDATION_JA.md](VALIDATION_JA.md)、履歴は[CHANGELOG.md](CHANGELOG.md)を参照する。

システム・開発者指示と実行権限の範囲内で「現在のユーザーの明示的な依頼 → リポジトリ固有の安全・品質ルール → Skill / AGENTS.mdの一般手順」の順に扱う。依頼範囲の拡大や安全機構の変更許可を推測しない。

- コード・テスト・Git履歴・周辺実装・文書から判断できる軽微な不明点は、既存仕様を保つ最小変更で進め、仮定を報告する。許可済み作業の再確認は不要。
- 未承認の売買仕様変更、破壊的変更、重大な結果の違いを生む未解決の選択は確認する。回答に依存しない調査・検証は続ける。範囲外の重大不具合は勝手に直さず報告する。
- 指示による停止・確認・依頼からの逸脱時は、ファイルパスと該当ルールを引用し、理由と必要な判断を示す。環境・権限不足とは区別する。
- Skillは用途・適用外を限定し、適合時だけ使う（同梱なし）。一般的な計画承認・毎回の全テスト等を上位の依頼へ追加しない。ルール追加前に既存表現の利用・統合・置換を検討する。重複・古い指示・矛盾・過剰な詳細/毎回実行を見つけた時だけ、依頼を妨げず安全に整理する。安全ルールは削らない。
- 調査は変更対象・直接関連のdiff・変更一覧・検索・履歴・関数/節を優先し、不足時だけ依存先へ広げる。必要な全文確認は省かない。未変更ファイルを理由なく全文再読しない。根拠が揃えば実装・レビュー・報告へ進み、読込量・実行回数を品質の代替にしない。

## EA固有の保護

明示的に依頼された変更以外では、以下の挙動・互換性を維持する。

- M1エントリー、10パターン、最高PatternScore選択、反対方向競合の拒否、同一構造の再利用防止。
- 7時間足の重み20/25/22/15/10/5/3、Trend/MACD 70/30、最終スコア80/15/5、MinimumSignalScore=70、方向一致の初期下限55%。
- 3つのリスクモード（Monte Carlo含む）、総ポートフォリオリスク上限、Fintokei保護、SL構造アンカーとfallback、ロット・TP・コスト判定、エントリー・決済・注文処理。
- 銘柄別状態、1銘柄1M1バーの発注制約、Magic/銘柄の所有判定、Netting/Hedging、未解決注文停止、口座ミューテックス。
- Workerプロトコル242、厳格JSON schema、AI期限・価格ドリフト・FailOpen条件。Workerから注文を出さない。
- 安全状態の永続化、分析ログ失敗と売買判断の分離、CSV列、PositionIdentifier照合、初期リスクR。インジケーター判定、UI、パラメータも無断変更しない。

ソース変更では本体・Worker・ヘッダー・入力・保存キー・ログ・移行処理の影響範囲を確認する。実MQL5仕様をmockより優先し、不整合はmock側を修正する。テストのためだけに製品仕様を変えない。秘密情報・APIキー・口座情報・実ログをソース、fixture、CIログ、artifactへ含めず、通信fixtureには架空値を使う。

## 開発・Git・レビュー

1. Gitの状態・基点・適用指示・関連履歴を確認する。GitHubのmainを正本とし、直接変更せず専用branchで作業する。既存の作業変更は保全する。ZIPの場合もGitHubとの対応と反映の制約を明記する。
2. 必要最小限の変更とし、無関係な機能削除・大規模リファクタリングを混ぜない。バグは根本原因を調べ、可能なら修正前に再現・回帰テストを追加する。既存テストは削除・弱体化しない。
3. 関連テスト・diffと保護への副作用をセルフレビューし、branchにコミットしてmain向けPRを作る。CI・レビュー結果を確認する。途中・完了報告は理由・範囲・件数・失敗・重要警告・未検証を要約し、重複説明・成功例の列挙・ログ転載を避ける。詳細ログは調査に必要な箇所だけ確認する。
4. PR作成はマージ・実口座導入の許可を意味しない。マージの明示的な依頼がある場合に、CI・レビューと変更に必要な実機確認を満たして実施する。

## テスト・実機検証

- テストは影響範囲に応じ「直接関連 → 関連 → 必要な統合 → 全体」へ広げる。文書のみならリンク・コマンド・diff・チェックサムを確認し、新しい形式的なランタイム回帰テストの追加は不要。ただしPR完成前の既存全体ゲートは次項どおり確認する。必要な安全確認・レビュー・テスト・回帰確認・実機未検証の記録は省かない。
- PR完成前・マージ前は `python3 tests/run_all.py` で全体検証する。同一コミット・差分・前提の信頼できるCI成功は再利用し、理由なく重複実行しない。コードや前提が変われば旧結果を流用せず再検証する。個別手順は[検証手順](VALIDATION_JA.md#再現配布結果)を参照。
- 失敗は実装不具合・テスト不具合・mock不整合・環境依存・既知の制限に切り分け、根拠を記録する。ツール不足時も可能な静的検証・Pythonテスト・コードレビューを続け、残るmock等はCIで補う。未実行・未検証を合格や実装の失敗に数えない。全体検証未完了のPRはdraftとする。
- MQL5変更では利用可能なMetaEditorネイティブコンパイル（目標0 errors / 0 warnings）と、関連するStrategy Tester・実機確認を優先する。文書変更だけで一律に実機試験を要求しない。
- Testerと実運用の差、再初期化・再起動・再接続、Global Variable・mutex・owner lockの実MQL5 semanticsを考慮する。警告は原因を調べ、依頼範囲内で最小修正する。
- MetaEditor/MT5が利用できないこと自体を失敗扱いしない。未実施なら「ネイティブコンパイル未実測」と記載する。C++17 mock・静的検証の成功を、ネイティブ、実機、市場バックテスト、実AI/API通信、収益性の成功と混同しない。

## 生成物・配布

verification/の同梱結果は過去の記録。更新時は実行結果と対象ソースSHAを対応させる。生成物（verification/*.cpp、integration / json_testとその.exe、case.json / initial.json、__pycache__/）をコミットしない。

配布対象の追加・変更時は、SHA256SUMS.txt自身を除く追跡対象ファイルのSHA-256をパス順に更新する。生成物を紛れ込ませず、意図した配布ファイルだけを含める。
